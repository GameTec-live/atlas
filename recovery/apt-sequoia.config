# Raspberry Pi's archive key still carries SHA-1 self-certifications. This is
# scoped to the pi-gen-micro build command; artifact integrity remains SHA-256.
[hash_algorithms]
sha1.second_preimage_resistance = 2030-01-01

